# Inactive Skills

这里保存当前目标暂时用不到的 skills。默认入口和搜索必须忽略本目录；需要恢复某项能力时，将文件移回 `rules/skills/` 并重新加入 `rules/skills/INDEX.md`。

OpenReview、1Password 已移出启用集合。
已删除的 PDF 转换能力不再保留归档。核心 rules 和 M07、M10 未因这些清理而变更。
